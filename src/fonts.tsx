import { Inter, Poiret_One } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});

export const poiret = Poiret_One({
  weight: "400",
  subsets: ["latin"],
  display: "swap",
});
